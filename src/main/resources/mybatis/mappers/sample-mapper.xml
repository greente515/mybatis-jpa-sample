<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.sample.sample.persistence.mapper.SampleMapper">

    <select id="findById1" resultType="sample" parameterType="Long">
        SELECT * FROM sample1 WHERE id = #{id}
    </select>

    <insert id="insert" parameterType="sample">
        insert into sample1 (name) values (#{name})
    </insert>

</mapper>